import styled from 'styled-components';

export const Container = styled.aside`
    display: flex;
  margin: 1px;
  flex: 1 1 200px;
  flex-wrap: wrap;
  justify-content: center;
  button {
    height: 3rem;
    width: 3rem;
  }

`;
